Install guide:
1. Clone the repo:
    git clone git://github.com/szorti/vim-config.git ~/.vim
2. Enter the .vim folder:
    cd ~/.vim 
3. Run the apply.sh script (copies font and link vimrc file)
    ./apply.sh 

Happy GVIMing!

